{
  "name": "angel-tree-donation-stream-email-admins",
  "version": "1.0.0",
  "description": "",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1.0",
    "@aws-sdk/client-sns": "^3.1.0",
    "@aws-sdk/util-dynamodb": "^3.1.0"
  },
  "scripts": {
    "init": "cp init/* ./ && npm i && sam build && sam deploy --guided",
    "deploy": "sam build && sam deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
