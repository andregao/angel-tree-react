{
  "name": "angel-tree-aws-api",
  "version": "1.0.0",
  "description": "",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^1.0.0-rc.7",
    "@aws-sdk/util-dynamodb": "^1.0.0-rc.7",
    "dotenv": "^8.2.0"
  },
  "scripts": {
    "sam": "sam build && sam local start-api --port 3001",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
