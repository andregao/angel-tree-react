{
  "name": "angel-tree-aws",
  "version": "1.0.0",
  "description": "",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^1.0.0-rc.6",
    "@aws-sdk/util-dynamodb": "^1.0.0-rc.6"
  },
  "scripts": {
    "dynamodb": "docker-compose up",
    "dynamodb-check": "aws dynamodb list-tables --endpoint-url http://127.0.0.1:8000",
    "sam": "sam build && sam local start-api --port 3001",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
