{
  "name": "angel-tree-aws-root",
  "version": "1.0.0",
  "description": "contains scripts to spin up dynamodb local",
  "scripts": {
    "db": "docker-compose up",
    "db-check": "aws dynamodb list-tables --endpoint-url http://127.0.0.1:8000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
